# Telegram бот "Дизрапт"

Интерактивная бизнес-игра для обучения маркетинговым стратегиям и управлению кризисами.

## Описание

Бот предоставляет:
- 🎮 Игровой процесс с генерацией случайных сценариев
- 🤖 AI-оценку решений через GigaChat
- 👥 Систему команд и профилей пользователей
- 📊 Просмотр решений для модераторов
- ⚙️ Админ-панель для управления

## Требования

- Docker и Docker Compose
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- GigaChat API credentials (получить на [developers.sber.ru](https://developers.sber.ru/portal/products/gigachat))

## Быстрый старт

### 1. Клонирование проекта

```bash
git clone <repository-url>
cd Disrupt
```

### 2. Настройка переменных окружения

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env и заполните:
# - BOT_TOKEN - токен вашего бота
# - ADMINS - ваш Telegram user_id (узнать можно у @userinfobot)
# - GIGACHAT_CREDENTIALS - ваши credentials от GigaChat
nano .env
```

### 3. Создание директории для данных

```bash
mkdir -p bot/data
```

### 4. Запуск бота

```bash
# Запуск в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка бота
docker-compose down
```

## Разработка

### Локальный запуск без Docker

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск бота
python -m bot.app
```

### Структура проекта

```
Disrupt/
├── bot/
│   ├── app.py                 # Точка входа
│   ├── config.py              # Настройки
│   ├── routers/               # Обработчики команд
│   │   ├── start.py           # /start и главное меню
│   │   ├── game.py            # Игровой процесс
│   │   ├── solutions.py       # Просмотр решений
│   │   ├── admin.py           # Админ-панель
│   │   └── profile.py         # Управление профилем
│   ├── services/              # Бизнес-логика
│   ├── keyboards/             # UI клавиатуры
│   ├── states/                # FSM состояния
│   └── data/                  # JSON база данных
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── .env
```

## Основные команды бота

- `/start` - Запуск бота и главное меню
- **Начать игру** - Начать новый раунд или зарегистрироваться
- **Решения** - Просмотр своих решений
- **Профиль** - Редактирование профиля (ФИО, город, команда)
- **Админ панель** - Управление модераторами (только для админов)

## Игровой процесс

1. Пользователь регистрируется (ФИО, город, название команды)
2. Получает случайный сценарий (целевая аудитория + продукт + канал продвижения)
3. Разрабатывает маркетинговую стратегию (минимум 300 символов)
4. Получает кризисное ограничение (случайное)
5. Адаптирует стратегию под новые условия
6. GigaChat оценивает оба решения и выдает развернутый фидбек

## Управление

### Добавление модератора

Админы могут назначать модераторов через "Админ панель":
- По @username
- По Telegram user_id

Модераторы получают доступ к просмотру всех решений с фильтрацией.

### База данных

Все данные хранятся в JSON файлах в директории `bot/data/`:
- `users.json` - профили пользователей
- `teams.json` - команды
- `rounds.json` - игровые раунды
- `solutions.json` - решения участников
- `moderators.json` - список модераторов

**Важно:** При использовании Docker эти файлы сохраняются между перезапусками благодаря volume mount.

## Обновление бота

```bash
# Остановка текущего контейнера
docker-compose down

# Обновление кода
git pull

# Пересборка и запуск
docker-compose up -d --build
```

## Логи и отладка

```bash
# Просмотр логов в реальном времени
docker-compose logs -f bot

# Последние 100 строк логов
docker-compose logs --tail=100 bot

# Вход в контейнер для отладки
docker-compose exec bot bash
```

## Резервное копирование

```bash
# Создание резервной копии данных
tar -czf backup-$(date +%Y%m%d).tar.gz bot/data/

# Восстановление из резервной копии
tar -xzf backup-20240119.tar.gz
```

## Возможные проблемы

### Бот не отвечает
- Проверьте логи: `docker-compose logs -f`
- Убедитесь, что BOT_TOKEN правильный
- Проверьте, запущен ли контейнер: `docker-compose ps`

### Ошибки GigaChat
- Проверьте GIGACHAT_CREDENTIALS
- Убедитесь, что GIGACHAT_SCOPE корректный
- Попробуйте установить `GIGACHAT_VERIFY_SSL=false`

### Потеря данных
- Убедитесь, что volume смонтирован правильно в docker-compose.yml
- Проверьте права доступа к директории bot/data/

## Лицензия

Проект разработан для Сбера.

## Контакты

По вопросам обращайтесь к разработчикам проекта.
